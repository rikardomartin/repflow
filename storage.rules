rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Regra básica: Apenas usuários autenticados podem ler/escrever
      allow read, write: if request.auth != null;
    }
  }
}
